package hello.colgoogle;

/*import com.google.common.collect.MapMaker;
import com.google.common.base.Function;

import java.util.concurrent.ConcurrentMap;
import java.util.concurrent.TimeUnit;

public class MapMakeT {
    MapMaker mapMaker = new MapMaker();
    ConcurrentMap<String, String> capthcaMap= mapMaker.expiration(15, TimeUnit.MINUTES).makeMap();

    ConcurrentMap<String, Object> map1 = mapMaker.concurrencyLevel(8).makeMap();

    ConcurrentMap<String, Object> map2 = mapMaker.softKeys().weakKeys().makeMap();

    ConcurrentMap<String, Integer> map3 = mapMaker.makeComputingMap( new Function<String, Integer>() {
        public Integer apply(String key) {
            return 1;
        }
    });
}*/
